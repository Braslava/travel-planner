// select the form
export const form = document.querySelector('.app__form');
// seect the loader
export const loader = document.querySelector('.loader');
// select the submit button
export const submitButton = document.querySelector('.submit-btn');
